import s from "./style.module.css";

function Experience() {
   return (
      <section id="experience" className={s.container}>
         <h3 className={s.title}>Expérience</h3>
      </section>
   );
}

export default Experience;
